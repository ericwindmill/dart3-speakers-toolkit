import 'package:dart_3_speakers_toolkit/user.dart';
import 'package:flutter/material.dart';

class AuthorWidget extends StatelessWidget {
  const AuthorWidget(this.user, {super.key});

  final User user;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class AdminWidget extends StatelessWidget {
  const AdminWidget(this.user, {super.key});

  final User user;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BlueCheckmarkWidget extends StatelessWidget {
  const BlueCheckmarkWidget(this.user, {super.key});

  final User user;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
